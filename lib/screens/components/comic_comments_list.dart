import 'package:flutter/material.dart';

class ComicCommentsList extends StatelessWidget {
  final int comicId;

  const ComicCommentsList(this.comicId, {Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Text("未完成");
  }
}
